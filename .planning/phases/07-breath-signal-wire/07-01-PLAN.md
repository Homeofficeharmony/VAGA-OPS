---
phase: 07-breath-signal-wire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - regulation-station/src/components/ImmersionContainer.jsx
  - regulation-station/src/App.jsx
  - regulation-station/src/components/ParticleField.jsx
autonomous: true
requirements:
  - VATM-02

must_haves:
  truths:
    - "During immersion stabilize phase, background particles visibly accelerate on inhale and slow on exhale"
    - "breathPhase in App.jsx is a live state value updated by ImmersionContainer callback, not hardcoded 'inhale'"
    - "Particle speed transitions ramp over ~1-2 seconds rather than snapping instantly"
    - "Particle speed resets to baseline when immersion closes"
  artifacts:
    - path: "regulation-station/src/components/ImmersionContainer.jsx"
      provides: "onBreathPhaseChange callback prop fires on each phase transition during stabilize"
    - path: "regulation-station/src/App.jsx"
      provides: "breathPhase state wired to ImmersionContainer callback and passed live to NeuralBackground"
    - path: "regulation-station/src/components/ParticleField.jsx"
      provides: "speedMultRef lerp toward breathSpeedMult target each rAF frame"
  key_links:
    - from: "regulation-station/src/components/ImmersionContainer.jsx"
      to: "regulation-station/src/App.jsx"
      via: "onBreathPhaseChange(breathPhase) callback prop"
    - from: "regulation-station/src/App.jsx"
      to: "regulation-station/src/components/NeuralBackground.jsx"
      via: "breathPhase={breathPhase} prop (replaces hardcoded string)"
    - from: "regulation-station/src/components/ParticleField.jsx"
      to: "rAF draw loop"
      via: "speedMultRef.current lerp toward breathSpeedMult[breathPhaseRef.current]"
---

<objective>
Thread the live breathPhase signal from ImmersionContainer up to App.jsx via callback, then back down to NeuralBackground and ParticleField. Add smooth lerp interpolation to the ParticleField rAF loop so particles ramp speed over ~1-2 seconds rather than snapping.

Purpose: VATM-02 requires particles to respond to breath phase. The signal already exists inside ImmersionContainer but is never surfaced to App.jsx — NeuralBackground has received a hardcoded "inhale" string since Phase 4.

Output: Three modified files. ImmersionContainer fires a callback on breath transitions. App.jsx holds breathPhase state and passes it live. ParticleField lerps speed smoothly toward the phase target.
</objective>

<execution_context>
@C:/Users/tyler/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tyler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-breath-signal-wire/07-CONTEXT.md
@.planning/phases/07-breath-signal-wire/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts extracted from the codebase for executor reference. -->
<!-- Do not explore the codebase to discover these — use the contracts below directly. -->

From regulation-station/src/components/ImmersionContainer.jsx (relevant props and internals):
- breathPhase computed at line 311 via useImmersionBreath hook: values are 'inhale' | 'hold' | 'exhale'
- Existing phase-transition useEffect at ~line 316:
  ```jsx
  useEffect(() => {
    if (phase !== 'stabilize') return
    const durationMs = timing[breathPhase] ?? 4000
    ambientEngine?.syncBreath(breathPhase, durationMs)
    if (breathPhase === 'inhale') hapticInhale()
    else if (breathPhase === 'exhale') hapticExhale()
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [breathPhase, phase])
  ```
- onComplete, onClose already wired as callback props — same pattern for onBreathPhaseChange
- phase values: 'welcome' | 'stabilize' | 'integrate'

From regulation-station/src/App.jsx:
- Line 250 (approximately): `<NeuralBackground ... breathPhase="inhale" />` — hardcoded string to replace
- ImmersionContainer rendered with: open={isImmersive}, stateData, ambientEngine, onComplete, onClose
- isImmersive boolean state already exists

From regulation-station/src/components/ParticleField.jsx:
- breathPhaseRef pattern already implemented (lines 90-95):
  ```jsx
  const breathPhaseRef = useRef(breathPhase)
  useEffect(() => {
    breathPhaseRef.current = breathPhase
  }, [breathPhase])
  ```
- PARTICLE_CONFIG has breathSpeedMult per state (existing Phase 4 values to preserve):
  - frozen: { inhale: 1.15, hold: 1.15, exhale: 0.55 }
  - anxious: { inhale: 1.45, hold: 1.45, exhale: 0.70 }
  - flow: { inhale: 1.20, hold: 1.20, exhale: 0.80 }
- Inside the rAF draw() function: currently uses breathSpeedMult with instant snap (no lerp)
- init useEffect depends only on [selectedState] — do NOT add breathPhase to this array
- speedMultRef does not yet exist — must add as useRef(1.0)
- init useEffect is the one that spawns the rAF loop — reset speedMultRef.current = 1.0 at its top

From regulation-station/src/components/NeuralBackground.jsx:
- Accepts breathPhase prop (default 'inhale'), passes it into ParticleField
- No changes needed to NeuralBackground itself
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onBreathPhaseChange callback to ImmersionContainer</name>
  <files>regulation-station/src/components/ImmersionContainer.jsx</files>
  <action>
    1. Add `onBreathPhaseChange` to the component's props destructuring alongside the existing onComplete and onClose props. Accept it with optional chaining default (no defaultProp needed — will use optional chaining when calling).

    2. Locate the existing phase-transition useEffect (the one that calls ambientEngine?.syncBreath and hapticInhale/hapticExhale). It already has the guard `if (phase !== 'stabilize') return`. Add a single line calling `onBreathPhaseChange?.(breathPhase)` immediately after the ambientEngine?.syncBreath call. Use optional chaining so the prop is safely absent when not provided (backward-compatible — no other call site needs to change).

    The effect already fires only when breathPhase changes AND phase === 'stabilize', which is exactly the right guard. Do not add a new useEffect — piggyback on the existing one.

    Result: ImmersionContainer fires the callback once per breath phase transition during stabilize, not on every rAF tick.
  </action>
  <verify>
    <automated>cd "C:/Users/tyler/nervous system ops pack/regulation-station" && npm run lint 2>&1 | grep -E "ImmersionContainer|error" | head -20</automated>
  </verify>
  <done>ImmersionContainer accepts onBreathPhaseChange prop and calls it on each breathPhase transition during stabilize phase only. No lint errors introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Lift breathPhase to App.jsx state and wire live prop</name>
  <files>regulation-station/src/App.jsx</files>
  <action>
    1. Add a new state declaration near the other useState declarations:
       `const [breathPhase, setBreathPhase] = useState('inhale')`

    2. Add a reset useEffect that returns breathPhase to 'inhale' when immersion closes:
       ```jsx
       useEffect(() => {
         if (!isImmersive) setBreathPhase('inhale')
       }, [isImmersive])
       ```

    3. In the ImmersionContainer JSX, add the new prop:
       `onBreathPhaseChange={setBreathPhase}`

    4. In the NeuralBackground JSX, replace the hardcoded string with the live state:
       - Before: `breathPhase="inhale"`
       - After: `breathPhase={breathPhase}`

    No other changes. Do not modify any other props or logic.
  </action>
  <verify>
    <automated>cd "C:/Users/tyler/nervous system ops pack/regulation-station" && npm run lint 2>&1 | grep -E "App\.jsx|error" | head -20</automated>
  </verify>
  <done>App.jsx holds breathPhase state initialized to 'inhale', resets on immersion close, passes setBreathPhase as onBreathPhaseChange to ImmersionContainer, and passes live breathPhase to NeuralBackground. No lint errors introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Add speedMultRef lerp to ParticleField rAF loop</name>
  <files>regulation-station/src/components/ParticleField.jsx</files>
  <action>
    1. Add a new ref alongside the existing breathPhaseRef declaration:
       `const speedMultRef = useRef(1.0)`

    2. Inside the init useEffect (the one with [selectedState] in its dep array), at the very top of the effect body — before the particle array construction and before the draw() call — add:
       `speedMultRef.current = 1.0`
       This prevents stale speed values after switching states.

    3. Inside the draw() function, locate where the current particle speed multiplier is applied (the line that reads from breathSpeedMult for the current phase). Replace that instant-snap line with the lerp pattern:
       ```jsx
       const targetMult = effectiveConfig.breathSpeedMult[breathPhaseRef.current] ?? 1.0
       speedMultRef.current += (targetMult - speedMultRef.current) * 0.025
       const speedMult = speedMultRef.current
       ```
       The lerp factor 0.025 at 60fps reaches 95% of target in ~1.9 seconds — within the locked "~1-2 seconds" decision.

    Preserve existing breathSpeedMult values per state (frozen: 1.15/0.55, anxious: 1.45/0.70, flow: 1.20/0.80) — these are Phase 4 design decisions and fall within the locked 1.5x ceiling.

    Do NOT add breathPhase or speedMultRef to the init useEffect dependency array. Keep [selectedState] as the only dep — adding breathPhase would destroy and rebuild all particles on every breath transition (the pitfall documented in RESEARCH.md).
  </action>
  <verify>
    <automated>cd "C:/Users/tyler/nervous system ops pack/regulation-station" && npm run lint 2>&1 | grep -E "ParticleField|error" | head -20</automated>
  </verify>
  <done>ParticleField has speedMultRef initialized at 1.0, reset at top of init useEffect, and lerped toward breathSpeedMult target each rAF frame using factor 0.025. No lint errors. Existing breathSpeedMult per-state values preserved.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. Run `npm run lint` from regulation-station/ — zero new errors.
2. Run `npm run build` from regulation-station/ — clean build, no TypeScript/module errors.
3. Manual: Open app at localhost:5173, select any state, open immersion session. Reach stabilize phase. Observe particle field — speed should visibly ramp up on inhale (1.2-1.5x depending on state) and ramp back down on exhale over ~2 seconds each transition.
4. Manual: Open React DevTools while in stabilize phase — confirm App.jsx breathPhase state value cycles 'inhale' → 'hold' → 'exhale' in sync with breath rhythm.
5. Manual: Close immersion session — confirm App.jsx breathPhase resets to 'inhale'.
</verification>

<success_criteria>
- breathPhase="inhale" hardcoded string no longer exists at App.jsx line 250
- ImmersionContainer accepts and calls onBreathPhaseChange prop
- ParticleField speedMultRef lerps toward target each frame (no instant snap)
- Particles visibly change speed during immersion stabilize phase
- Zero new lint errors introduced
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-breath-signal-wire/07-01-SUMMARY.md`
</output>
